extends ../shared/template

block content
  each product in products
    .row
      .large-8.columns
        ul.no-bullets
          li
            a(href='/products/#{product._id.toString()}')= product.name
